var swiper = new Swiper('.swiper-wrapper', {
    // Optional Swiper options (see Swiper documentation):
    // https://swiperjs.com/swiper-api
    slidesPerView: 'auto', // Adapt slides to container width
    spaceBetween: 20 ,// Adjust spacing between slides (optional)
    mousewheel: true,
});
